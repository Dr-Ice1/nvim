# nvim
lua based nvim config, doesn't work (yet)


Pyright --> "Client quit with exit code 1"

Auto-completion --> Only works in the plugin folder, outside of that it only picks up text from the buffer

No idea how to set it up for LaTeX
